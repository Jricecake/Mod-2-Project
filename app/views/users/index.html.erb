<h1>User Page</h1>

<% if User.first != nil %>
<ul><label>Select a User:</label>
<% @users.each do |user| %>
    <li><%= link_to user.name, user %></li>
    <% end %>
    </ul><br>
    <% else %>
<p>No users yet!</p>
<% end %>

<%= link_to "Create User", new_user_path %>

